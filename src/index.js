import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';
//import Stateuse from './components/UseState/stateuse'; // Assuming 'stateuse' is a custom component

ReactDOM.createRoot(document.getElementById('root')).render(
  // <React.StrictMode>
    <App />
    // <Stateuse />
  // </React.StrictMode>
);